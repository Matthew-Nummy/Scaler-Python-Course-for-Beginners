class Solution:
    # @param A : integer
    # @return an integer
    def solve(self, A):
        #A = int(input())
        if A % 400 == 0:
            B = 1
        elif A % 4 == 0 and A % 100 != 0:
            B = 1
        else:
            B = 0
            
        return B
            

